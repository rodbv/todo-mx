[project]
name = "todo-mx"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1.4",
]

[dependency-groups]
dev = [
    "pytest>=8.3.4",
    "pytest-sugar>=1.0.0",
]
